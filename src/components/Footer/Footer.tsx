import styles from "./Footer.module.scss";

const Footer = () => {
    return (
        <div className={styles.footer}>
            <p className="text-red-800">Footer</p>
        </div>
    );
};

export default Footer;