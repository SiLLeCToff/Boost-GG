import styles from "./Footer.module.scss";

const Footer = () => {
    return (
        <div className={styles.footer}>
            <h1 className="text-red-800">Footer</h1>
        </div>
    );
};

export default Footer;